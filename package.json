{
  "name": "sample-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "cd apps/summit && npm run dev",
    "build": "cd apps/summit && npm run build",
    "start": "cd apps/summit && npm run start",
    "lint": "cd apps/summit && npm run lint"
  }
}
